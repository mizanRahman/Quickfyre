<%= form_for(@laccount) do |f| %>
  <% if @laccount.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@laccount.errors.count, "error") %> prohibited this laccount from being saved:</h2>

      <ul>
      <% @laccount.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
